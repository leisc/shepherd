# Experiments

The experiment code for the Shepherd paper lives in a separate repository:

**https://github.com/shepherd-agents/shepherd-experiments**

It holds the four meta-agent applications and the framework-performance
microbenchmarks from the paper, along with the frozen substrate snapshot they
were run against. Keeping it separate lets this repository track the actively
maintained library while the paper's numbers stay reproducible against the
pinned version.
